---
description: 完全安全模式：破坏性命令警告 + 目录限定编辑。当用户说「guard」「guard mode」「安全模式全开」「full safety」「lock it down」「maximum safety」时使用。组合 /careful + /freeze 的双重保护。
alwaysApply: false
enabled: true
updatedAt: 2026-03-25T04:12:03.814Z
provider: 
---

# /guard — 完全安全模式

激活破坏性命令警告 + 目录限定编辑。这是 /careful + /freeze 的联合模式。

## 初始设置

询问用户限制编辑到哪个目录。破坏性命令警告默认全局开启。

## 双重保护

1. **破坏性命令警告**（/careful）— rm -rf、DROP TABLE、force-push 等命令执行前警告
2. **编辑边界限制**（/freeze）— 冻结目录外的文件编辑被阻止

## 检查流程

- 终端命令 → 执行 /careful 完整检查
- 文件编辑 → 执行 /freeze 完整检查

使用 /unfreeze 仅解除编辑边界（命令警告仍保持）。结束会话停用所有保护。