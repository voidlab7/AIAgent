---
description: 限制编辑范围到指定目录。当用户说「freeze」「冻结」「限制编辑」「只编辑这个文件夹」「restrict edits」「lock down edits」时使用。冻结边界外的文件编辑将被阻止。
alwaysApply: false
enabled: true
updatedAt: 2026-03-25T04:12:03.777Z
provider: 
---

# /freeze — 限制编辑范围到指定目录

将文件编辑锁定到指定目录。任何对该路径外文件的编辑操作将被**阻止**。

## 核心行为

**铁律：冻结边界外的文件 → 拒绝编辑 → 不询问 → 直接告知用户。**

## 初始设置

激活时询问用户要限制编辑到哪个目录。解析为绝对路径并记录。

## 检查流程

在使用 replace_in_file 或 write_to_file 前：
1. 提取文件路径
2. 检查是否以冻结目录开头
3. 在边界内 → 允许；在边界外 → 阻止

阻止格式：
```
🚫 [freeze] 已阻止: {file_path}
此文件在冻结边界 ({FREEZE_DIR}/) 外。
```

## 说明
- 仅影响文件编辑工具（replace_in_file、write_to_file）
- 读取、搜索、终端命令不受影响
- 使用 /unfreeze 解除